<% if @flag %>
  window.location.href = "/ofertas"
<% else %>
  $(".registration-errors").html("<%= escape_javascript(render("new")) %>");
  $(document).ready(function() {
    setTimeout(function() {
      $(".registration-errors .devise-errors").slideUp();
    }, 4000);
  });
<% end %>